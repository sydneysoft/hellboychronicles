(()=>{const root=document.getElementById('story');if(!root)return;const source=/^\/pl(?:\/|$)/.test(location.pathname)?'pl':'en',target=source==='pl'?'en':'pl';const cache=new Map();const style=document.createElement('style');style.textContent=`.plv-pop{position:fixed;z-index:9999;width:min(330px,calc(100vw - 24px));background:#171311;color:#f5e9cf;border:1px solid #c4a255;border-radius:16px;box-shadow:0 18px 50px #0009;padding:14px;font-family:system-ui,-apple-system,sans-serif}.plv-top{display:flex;align-items:flex-start;justify-content:space-between;gap:10px}.plv-word{font:800 20px Georgia,serif;color:#fff}.plv-close{border:0;background:transparent;color:#b9ab96;font-size:20px;cursor:pointer}.plv-label{margin-top:3px;color:#d7b65c;font-size:10px;font-weight:800;letter-spacing:.13em;text-transform:uppercase}.plv-translation{margin-top:10px;font-size:18px;line-height:1.35}.plv-loading{color:#aaa;font-style:italic}.plv-actions{display:flex;gap:8px;margin-top:12px}.plv-actions button{flex:1;border:1px solid #b99a5355;border-radius:10px;padding:9px 10px;background:#2a211d;color:#f4e5c8;font-weight:800;cursor:pointer}.plv-actions button:first-child{background:#7f1d27}.plv-error{color:#d7b9a9;font-size:13px;line-height:1.35}`;document.head.appendChild(style);let pop=null,timer=null;function clean(s){return s.trim().replace(/^[\s“”‘’"'.,!?;:()\[\]{}—–-]+|[\s“”‘’"'.,!?;:()\[\]{}—–-]+$/g,'').replace(/\s+/g,' ')}function close(){if(pop){pop.remove();pop=null}}function speak(text){speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang=source==='pl'?'pl-PL':'en-US';speechSynthesis.speak(u)}function save(word,translation){const key='hellboy-polish-vocabulary';let items=[];try{items=JSON.parse(localStorage.getItem(key)||'[]')}catch{};if(!items.some(x=>x.word.toLowerCase()===word.toLowerCase()&&x.source===source))items.unshift({word,translation,source,target,savedAt:Date.now()});localStorage.setItem(key,JSON.stringify(items.slice(0,250)))}async function translate(text){const k=`${source}:${target}:${text.toLowerCase()}`;if(cache.has(k))return cache.get(k);const url=`https://api.mymemory.translated.net/get?q=${encodeURIComponent(text)}&langpair=${source}%7C${target}`;const r=await fetch(url,{headers:{Accept:'application/json'}});if(!r.ok)throw new Error('translation unavailable');const j=await r.json();const value=j?.responseData?.translatedText?.trim();if(!value)throw new Error('translation unavailable');cache.set(k,value);return value}function place(box){const margin=12,w=Math.min(330,innerWidth-24);let left=Math.max(margin,Math.min(innerWidth-w-margin,box.left+box.width/2-w/2));let top=box.bottom+10;if(top+210>innerHeight)top=Math.max(margin,box.top-210);pop.style.left=`${left}px`;pop.style.top=`${top}px`}async function show(){clearTimeout(timer);const sel=getSelection();if(!sel||sel.isCollapsed||!sel.rangeCount)return close();if(!root.contains(sel.anchorNode)||!root.contains(sel.focusNode))return;const word=clean(sel.toString());if(!word||word.length>80||word.split(/\s+/).length>6)return close();const rect=sel.getRangeAt(0).getBoundingClientRect();if(!rect.width&&!rect.height)return;close();pop=document.createElement('div');pop.className='plv-pop';pop.innerHTML=`<div class="plv-top"><div><div class="plv-word"></div><div class="plv-label">${source==='pl'?'POLISH → ENGLISH':'ENGLISH → POLISH'}</div></div><button class="plv-close" aria-label="Close">×</button></div><div class="plv-translation plv-loading">Translating…</div><div class="plv-actions"><button class="plv-save">SAVE WORD</button><button class="plv-speak">🔊 LISTEN</button></div>`;pop.querySelector('.plv-word').textContent=word;document.body.appendChild(pop);place(rect);pop.querySelector('.plv-close').onclick=close;pop.querySelector('.plv-speak').onclick=()=>speak(word);let translated='';try{translated=await translate(word);if(!pop)return;const t=pop.querySelector('.plv-translation');t.classList.remove('plv-loading');t.textContent=translated;pop.querySelector('.plv-save').onclick=e=>{save(word,translated);e.currentTarget.textContent='✓ SAVED'}}catch{if(pop)pop.querySelector('.plv-translation').innerHTML='<span class="plv-error">Translation service is temporarily unavailable. Try selecting the word again.</span>'}}root.addEventListener('mouseup',()=>timer=setTimeout(show,20));root.addEventListener('touchend',()=>timer=setTimeout(show,180),{passive:true});document.addEventListener('selectionchange',()=>{clearTimeout(timer);timer=setTimeout(()=>{const s=getSelection();if(s&&!s.isCollapsed&&root.contains(s.anchorNode))show()},420)});document.addEventListener('pointerdown',e=>{if(pop&&!pop.contains(e.target)&&!root.contains(e.target))close()});})();